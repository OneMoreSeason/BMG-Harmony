---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on:
  - "02-01 board mechanics"
  - "02-02 battle cards and roles"
files_modified:
  - server/schema.py
  - server/store.py
  - tests/test_phase3_dissent.py
autonomous: true
requirements:
  - DISSENT-01
must_haves:
  truths:
    - "file_dissent writes append-only dissent events; it does not update or delete existing events"
    - "Dissent category is required and must be technical, doctrine, or scope"
    - "Dissent records are attributed, timestamped, and readable through read_thread"
    - "Malformed categories fail before store mutation"
  artifacts:
    - path: "server/schema.py"
      provides: "Strict validation for structured dissent payloads"
    - path: "server/store.py"
      provides: "file_dissent store operation using the existing event log"
    - path: "tests/test_phase3_dissent.py"
      provides: "DISSENT-01 regression coverage"
---

<objective>
Add the structured dissent store operation while preserving the Phase 1 event
log as implementation truth.
</objective>

<execution_context>
@C:/Users/thepo/OneDrive/Documents/GitHub/BMG-Harmony/.planning/phases/03-structured-dissent-response-windows/03-CONTEXT.md
@C:/Users/thepo/OneDrive/Documents/GitHub/BMG-Harmony/server/schema.py
@C:/Users/thepo/OneDrive/Documents/GitHub/BMG-Harmony/server/store.py
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dissent payload validation</name>
  <files>server/schema.py</files>
  <behavior>
    - `category` is required.
    - Valid categories are exactly `technical`, `doctrine`, and `scope`.
    - `content_md` remains the human-readable objection body.
    - Optional future-facing fields such as `target_event_id` and
      `response_window_hours` are validated when present.
  </behavior>
  <action>
    Add a focused dissent validator using the existing Draft7Validator pattern.
    Keep validation separate from `EVENT_SCHEMA` so ordinary `message` and
    `position` events remain unchanged.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement file_dissent in store.py</name>
  <files>server/store.py, tests/test_phase3_dissent.py</files>
  <behavior>
    - `file_dissent(con, thread_id, agent_id, category, content_md)` writes a
      `kind='dissent'` event with a JSON payload containing `category`.
    - Receipt shape matches `write_event`: `{event_id, thread_id, revision,
      timestamp, validation_status}`.
    - Existing `read_thread` output shows the dissent event and payload.
    - Invalid categories raise ToolError and do not create an event.
  </behavior>
  <action>
    Implement `file_dissent` as a narrow wrapper around `write_event` after
    validating the category payload. Add tests for valid categories, invalid
    categories, receipt shape, and readback.
  </action>
</task>

</tasks>

<validation>
Run:

```powershell
"C:/Users/thepo/AppData/Local/Programs/Python/Python313/python.exe" -m pytest tests/test_phase3_dissent.py -v
"C:/Users/thepo/AppData/Local/Programs/Python/Python313/python.exe" -m pytest tests/test_store.py tests/test_phase2_board.py -v
```

Acceptance:
- DISSENT-01 behavior is covered by tests.
- Existing Phase 1/2 event reads still pass.
- No live `.harmony/store/harmony.sqlite` file is required for tests.
</validation>

<security>
No new network surface. Main risk is treating arbitrary payload JSON as
structured dissent. Mitigate with schema validation before writes and an
allowlist for categories.
</security>
