---
phase: "03"
plan: "02"
type: execute
wave: 1
depends_on:
  - "03-01 structured dissent records"
files_modified:
  - server/schema.sql
  - server/schema.py
  - server/store.py
  - tests/test_phase3_response_windows.py
autonomous: true
requirements:
  - DISSENT-02
must_haves:
  truths:
    - "confirm_delivery is explicit delivery proof for response-window consent rules"
    - "Before delivery confirmation, silence is no_response"
    - "After delivery confirmation but before expiry, silence is pending"
    - "After delivery confirmation plus elapsed response window, silence is recorded agreement"
    - "Agreement-by-silence has durable evidence, not an ambiguous missing reply"
  artifacts:
    - path: "server/store.py"
      provides: "confirm_delivery and response-window state-machine logic"
    - path: "server/schema.sql"
      provides: "Any needed append-only response-window/agreement records"
    - path: "tests/test_phase3_response_windows.py"
      provides: "DISSENT-02 state-machine coverage"
---

<objective>
Make silence semantics deterministic: no consent can be inferred until delivery
is confirmed and the configured response window has elapsed.
</objective>

<execution_context>
@C:/Users/thepo/OneDrive/Documents/GitHub/BMG-Harmony/.planning/phases/03-structured-dissent-response-windows/03-CONTEXT.md
@C:/Users/thepo/OneDrive/Documents/GitHub/BMG-Harmony/server/schema.sql
@C:/Users/thepo/OneDrive/Documents/GitHub/BMG-Harmony/server/store.py
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit delivery confirmation</name>
  <files>server/store.py, tests/test_phase3_response_windows.py</files>
  <behavior>
    - `confirm_delivery(con, message_id, agent_id)` returns an append-only
      delivery receipt.
    - It is idempotent per message/agent pair.
    - It shares durable storage with `ack_message` unless a separate table is
      necessary for evidence clarity.
    - Existing `ack_message` callers keep working.
  </behavior>
  <action>
    Implement `confirm_delivery` as the Phase 3 semantic entrypoint for the
    existing acknowledgement table. Prefer delegating to `ack_message` so there
    is one source of truth for delivery receipts.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Model response-window state</name>
  <files>server/schema.sql, server/store.py, tests/test_phase3_response_windows.py</files>
  <behavior>
    - Dissent records can carry or derive a response deadline.
    - State before delivery confirmation is `no_response`.
    - State after delivery confirmation and before deadline is `pending`.
    - State after delivery confirmation and after deadline is
      `agreement_by_silence`.
    - The calculation is deterministic and testable with injected timestamps.
  </behavior>
  <action>
    Add store helpers that compute response-window status from dissent events,
    delivery confirmations, and current time. If a durable agreement record is
    needed, add an append-only table or event kind-compatible record and protect
    it with update/delete triggers.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce thread state transitions</name>
  <files>server/store.py, tests/test_phase3_response_windows.py</files>
  <behavior>
    - Threads with unresolved dissent remain open.
    - Threads cannot be treated as agreed while any dissent is undelivered or
      pending.
    - Expired delivered dissent records surface agreement-by-silence with
      evidence fields.
    - State-machine code is store-layer deterministic logic, not MCP adapter
      policy.
  </behavior>
  <action>
    Add a narrow thread-state helper used by battle-card/list reads or explicit
    response-window reads. Preserve existing thread rows and avoid destructive
    mutation unless an append-only evidence record is being added.
  </action>
</task>

</tasks>

<validation>
Run:

```powershell
"C:/Users/thepo/AppData/Local/Programs/Python/Python313/python.exe" -m pytest tests/test_phase3_response_windows.py -v
"C:/Users/thepo/AppData/Local/Programs/Python/Python313/python.exe" -m pytest tests/test_phase2_board.py tests/test_phase2_battle_card.py -v
```

Acceptance:
- DISSENT-02 is proven for all three states: `no_response`, `pending`, and
  `agreement_by_silence`.
- Delivery confirmation idempotence is proven.
- Existing Phase 2 ack/read/list behavior remains intact.
</validation>

<security>
No external calls. Main risks are time-dependent flake and accidental consent.
Mitigate with injectable clocks in tests, explicit delivery checks, and
agreement evidence fields.
</security>
