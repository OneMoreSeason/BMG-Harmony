---
phase: "03"
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01 structured dissent records"
  - "03-02 response-window state machine"
files_modified:
  - server/harmony_server.py
  - server/store.py
  - tests/test_phase3_battle_card_mcp.py
  - SETUP.md
autonomous: false
requirements:
  - DISSENT-01
  - DISSENT-02
  - DISSENT-03
must_haves:
  truths:
    - "FastMCP exposes exactly the two new Phase 3 tools requested: file_dissent and confirm_delivery"
    - "MCP wrappers delegate to store functions and do not reason about consent"
    - "Battle cards expose dissent categories and response-window status on cold-open"
    - "A real MCP call can file dissent, confirm delivery, and read the resulting battle card"
  artifacts:
    - path: "server/harmony_server.py"
      provides: "FastMCP wrappers for Phase 3 tools"
    - path: "server/store.py"
      provides: "Battle-card dissent/window visibility"
    - path: "tests/test_phase3_battle_card_mcp.py"
      provides: "DISSENT-03 and MCP exposure coverage"
    - path: "SETUP.md"
      provides: "Updated operator notes only if tool names or wiring expectations change"
---

<objective>
Expose Phase 3 over MCP and make dissent/window state visible on lean battle
cards so cold-open agents see objections before acting.
</objective>

<execution_context>
@C:/Users/thepo/OneDrive/Documents/GitHub/BMG-Harmony/.planning/phases/03-structured-dissent-response-windows/03-CONTEXT.md
@C:/Users/thepo/OneDrive/Documents/GitHub/BMG-Harmony/server/harmony_server.py
@C:/Users/thepo/OneDrive/Documents/GitHub/BMG-Harmony/server/store.py
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FastMCP wrappers for Phase 3 tools</name>
  <files>server/harmony_server.py, tests/test_phase3_battle_card_mcp.py</files>
  <behavior>
    - `file_dissent(thread_id: str, agent_id: str, category: str,
      content_md: str) -> dict`
    - `confirm_delivery(message_id: str, agent_id: str) -> dict`
    - Tool list includes Phase 1, Phase 2, and Phase 3 tools.
    - Wrappers call store functions directly and return deterministic receipts.
  </behavior>
  <action>
    Extend the store import block and add one `@mcp.tool()` wrapper per Phase 3
    tool. Update MCP exposure tests to assert both tool names are listed.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dissent/window fields to battle cards</name>
  <files>server/store.py, tests/test_phase3_battle_card_mcp.py</files>
  <behavior>
    - `get_battle_card(thread_id)` includes compact dissent details, not only a
      count.
    - Fields include latest/open dissent category, status, delivery state, and
      response deadline when available.
    - `open_flags` distinguish undelivered dissent, pending response window,
      and agreement-by-silence evidence.
    - Lean card remains compact and does not embed transcript dumps.
  </behavior>
  <action>
    Add deterministic battle-card assembly for dissent records and response
    windows. Preserve existing keys so Phase 2 callers remain compatible.
  </action>
</task>

<task type="checkpoint" tdd="false">
  <name>Task 3: Dogfood Phase 3 dissent workflow</name>
  <files>runtime store only; optional SETUP.md note</files>
  <behavior>
    - Codex can file a small typed dissent on a test thread.
    - Delivery can be confirmed with `confirm_delivery`.
    - `get_battle_card` shows dissent category and window status.
    - Runtime store remains git-ignored.
  </behavior>
  <action>
    After implementation tests pass, use the configured bmg-harmony MCP route
    for a minimal dogfood call sequence. Do not commit the runtime SQLite store.
  </action>
</task>

</tasks>

<validation>
Run:

```powershell
"C:/Users/thepo/AppData/Local/Programs/Python/Python313/python.exe" -m pytest tests/ -v
codex mcp get bmg-harmony
```

Manual MCP proof, if needed:

```powershell
# Use the configured bmg-harmony MCP route to call:
# file_dissent, confirm_delivery, get_battle_card.
```

Acceptance:
- All Phase 3 tests pass with existing Phase 1/2 regressions.
- MCP tool list includes `file_dissent` and `confirm_delivery`.
- Battle-card cold-open shows dissent/window status without reading full
  history.
</validation>

<security>
No new auth or network surface. Main risks are stdout pollution, accidental
consent inference, and overlarge battle-card payloads. Mitigate with thin MCP
wrappers, store-layer state-machine tests, and compact card fields.
</security>
