---
phase: "02"
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01 board mechanics"
  - "02-02 battle cards and roles"
files_modified:
  - server/harmony_server.py
  - tests/test_phase2_mcp.py
  - SETUP.md
autonomous: false
requirements:
  - BOARD-03
  - BOARD-04
  - BOARD-05
  - CARD-01
  - CARD-02
  - CARD-03
  - ROLES-01
  - ROLES-02
must_haves:
  truths:
    - "FastMCP exposes exactly the four new Phase 2 tools requested: ack_message, reply_message, get_battle_card, append_proving_envelope"
    - "Each MCP tool delegates to the store layer and returns the store receipt or read shape"
    - "No stdout logging or print output is introduced"
    - "A real MCP stdio client can list and call the Phase 2 tools"
    - "SETUP.md reflects any required launch environment, including PYTHONPATH if script-path launch remains the chosen route"
  artifacts:
    - path: "server/harmony_server.py"
      provides: "FastMCP adapter for Phase 2 tools"
    - path: "tests/test_phase2_mcp.py"
      provides: "Tool exposure and MCP call smoke coverage"
    - path: "SETUP.md"
      provides: "Updated operator instructions only if Phase 2 changes wiring expectations"
---

<objective>
Wire Phase 2 store operations into the existing FastMCP server and prove the
shared board can be used through the same transport Claude and Codex already
dogfooded in Phase 1.
</objective>

<execution_context>
@C:/Users/thepo/OneDrive/Documents/GitHub/BMG-Harmony/server/harmony_server.py
@C:/Users/thepo/OneDrive/Documents/GitHub/BMG-Harmony/server/store.py
@C:/Users/thepo/OneDrive/Documents/GitHub/BMG-Harmony/SETUP.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FastMCP wrappers for Phase 2 tools</name>
  <files>server/harmony_server.py</files>
  <behavior>
    - `ack_message(message_id: str, agent_id: str) -> dict`
    - `reply_message(thread_id: str, parent_message_id: str, agent_id: str,
      kind: str, content_md: str) -> dict`
    - `get_battle_card(thread_id: str) -> dict`
    - `append_proving_envelope(thread_id: str, agent_id: str, proved: str,
      not_checked: str, confidence: float) -> dict`
    - Wrappers import store functions and pass `_db`.
    - No reasoning, summarization, or policy decision occurs in
      `harmony_server.py`.
  </behavior>
  <action>
    Extend the import block from `server.store` with the four Phase 2 store
    functions. Add one `@mcp.tool()` wrapper per requested MCP tool, matching
    Phase 1's docstring and return style.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP transport proof</name>
  <files>tests/test_phase2_mcp.py</files>
  <behavior>
    - Importing `server.harmony_server` succeeds.
    - FastMCP tool list includes Phase 1 tools plus the four new Phase 2 tools.
    - A stdio MCP client can call at least one mutation (`ack_message` or
      `append_proving_envelope`) and one read (`get_battle_card`) against a temp
      store.
    - Stderr logging remains safe for stdio transport.
  </behavior>
  <action>
    Add focused MCP smoke coverage. If full stdio client tests are too brittle,
    keep store-layer tests authoritative and add a bounded manual verification
    command to the summary.
  </action>
</task>

<task type="checkpoint" tdd="false">
  <name>Task 3: Dogfood Phase 2 board use</name>
  <files>runtime store only; optional SETUP.md note</files>
  <behavior>
    - Claude or Codex can ack the Phase 1 stack-position message.
    - Either agent can reply to the stack decision thread.
    - `get_battle_card("harmony-stack-decision")` returns a lean card.
    - `append_proving_envelope` records what the implementing agent proved and
      did not check.
  </behavior>
  <action>
    After implementation tests pass, use the configured bmg-harmony MCP route
    to post a tiny Phase 2 dogfood exchange in the runtime SQLite store. Do not
    commit the store file.
  </action>
</task>

</tasks>

<validation>
Run:

```powershell
"C:/Users/thepo/AppData/Local/Programs/Python/Python313/python.exe" -m pytest tests/ -v
codex mcp get bmg-harmony
```

Manual MCP proof, if needed:

```powershell
# Use the same Python MCP client pattern from Phase 1 Gate 4 to call:
# list_tools, append_proving_envelope, get_battle_card.
```

Acceptance:
- All Phase 1 and Phase 2 tests pass.
- `bmg-harmony` still lists as enabled in Codex MCP config.
- MCP call receipts include event/envelope/ack IDs and validation status.
- Runtime store remains git-ignored.
</validation>

<security>
No new auth, network, or destructive capabilities. The tool surface is still
local stdio over a trusted internal SQLite file. Main risks are accidental store
commit, stdout pollution, and tool-schema drift; mitigate with `.gitignore`,
stderr-only logging checks, and MCP list/call verification.
</security>
